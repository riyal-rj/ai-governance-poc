# Local-only values. Use a secret manager in deployed environments.
POSTGRES_PASSWORD=finassist_local_only

FINASSIST_ENVIRONMENT=local
FINASSIST_DEBUG=false
FINASSIST_DOCS_ENABLED=true
FINASSIST_LOG_JSON=false
