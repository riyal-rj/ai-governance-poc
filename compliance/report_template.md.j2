# AI Governance Compliance Report

Generated: {{ generated_at }}

This report is generated by `compliance/generate_report.py`, which pulls
each item's evidence live from the real service that produced it (OPA,
MLflow, OpenMetadata, Langfuse, the kill-switch log) — nothing below is
hand-written or simulated.

| Framework | Requirement | Status | Evidence |
|---|---|---|---|
{% for item in items -%}
| {{ item.framework }} | {{ item.requirement }} | {{ "PASS" if item.satisfied else "FAIL — no evidence found" }} | {{ item.evidence | replace("\n", "<br>") }} |
{% endfor %}

## Agent Governance Toolkit — OWASP Agentic Top 10 grading

Pulled directly from `agt verify` (not re-implemented):

```
{{ agt_verify_raw }}
```
